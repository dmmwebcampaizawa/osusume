<div class="container">
  <div class="row">
    <div class='mx-auto col-12 mt-4'>

      <div class="text-right">
        <% if user_signed_in? %>

          ようこそ <%= current_user.name %> さん｜<%= link_to "マイページ", my_page_path %>｜
          <%= link_to "ログアウト", destroy_user_session_path, method: :delete %>
          <div class="mt-3">
          <%= link_to "商品をおすすめする", new_blog_path, class: 'btn btn-warning' %>
          <div>
        <% else %>
          <%= link_to "新規会員登録", new_user_registration_path %>｜<%= link_to "ログイン", new_user_session_path %>
        <% end %>
      </div>

    <div class="mt-2">
      <%= link_to '/' do%>
        <img src="../assets/logo.png" class="rounded mx-auto d-block" alt="おすすめを教えてロゴ">
      <% end %>
    </div>

    <div class="border-bottom mt-4"></div>

    </div>
  </div>
</div>