<div class="container">
  <div class="row">
    <div class='mx-auto col-12 mt-4'>

      <div class="text-right">
        <% if admin_signed_in? %>
          <%= link_to "会員一覧", admin_users_path %>｜<%= link_to "記事一覧", admin_blogs_path %>｜
          <%= link_to "ログアウト", destroy_admin_session_path, method: :delete %>
          <div>
        <% else %>
        <% end %>
      </div>

    <div class="mt-2 text-center">
     <h1 class=>おすすめを教えて！管理者用サイト</h1>
    </div>

    <div class="border-bottom mt-4"></div>

    </div>
  </div>
</div>